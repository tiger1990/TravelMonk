<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Post-splash theme for dark mode -->
    <style name="Theme.TravelMonk.NoActionBar" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Launch theme for dark mode: matches BackgroundDark (#0E1110) -->
    <style name="Theme.TravelMonk" parent="Theme.SplashScreen">
        <!-- <item name="windowSplashScreenBackground">@color/background_dark</item> -->
        <!-- <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item> -->
        <item name="postSplashScreenTheme">@style/Theme.TravelMonk.NoActionBar</item>

        <!-- Make status bar transparent during splash to prevent grey flash -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
