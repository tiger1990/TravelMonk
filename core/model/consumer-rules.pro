# consumer-rules.pro for :core:model
#
# kotlinx.serialization ships its own bundled ProGuard rules that keep all
# @Serializable classes automatically — no manual rules needed here today.
#
# Add module-specific rules below when new reflection-dependent code is introduced.
