# consumer-rules.pro for :core:navigation
#
# TravelNavKey subtypes are annotated with @Serializable + @SerialName.
# kotlinx.serialization's bundled ProGuard rules protect them automatically.
#
# Add module-specific rules below when new reflection-dependent code is introduced.
