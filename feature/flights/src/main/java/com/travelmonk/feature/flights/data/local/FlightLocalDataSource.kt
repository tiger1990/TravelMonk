package com.travelmonk.feature.flights.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for caching flight search results.
//
// Example:
// @Dao
// interface FlightDao {
//     @Query("SELECT * FROM flights WHERE fromCity = :from AND toCity = :to")
//     suspend fun getCachedFlights(from: String, to: String): List<FlightEntity>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheFlights(flights: List<FlightEntity>)
// }
