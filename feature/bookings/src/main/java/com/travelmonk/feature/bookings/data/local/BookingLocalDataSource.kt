package com.travelmonk.feature.bookings.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for persisting bookings locally.
//
// Example:
// @Dao
// interface BookingDao {
//     @Query("SELECT * FROM bookings")
//     fun observeBookings(): Flow<List<BookingEntity>>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheBookings(bookings: List<BookingEntity>)
//
//     @Query("UPDATE bookings SET status = 'CANCELLED' WHERE id = :bookingId")
//     suspend fun cancelBooking(bookingId: String)
// }
