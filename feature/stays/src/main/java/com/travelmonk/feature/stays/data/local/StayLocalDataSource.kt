package com.travelmonk.feature.stays.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for caching stay search results.
//
// Example:
// @Dao
// interface StayDao {
//     @Query("SELECT * FROM stays WHERE location = :location")
//     suspend fun getCachedStays(location: String): List<StayEntity>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheStays(stays: List<StayEntity>)
// }
