package com.travelmonk.feature.home.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for caching home banners.
//
// Example:
// @Dao
// interface HomeBannerDao {
//     @Query("SELECT * FROM home_banners")
//     suspend fun getCachedBanners(): List<HomeBannerEntity>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheBanners(banners: List<HomeBannerEntity>)
// }
