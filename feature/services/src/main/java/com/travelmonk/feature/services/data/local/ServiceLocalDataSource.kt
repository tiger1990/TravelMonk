package com.travelmonk.feature.services.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for caching available services.
//
// Example:
// @Dao
// interface ServiceDao {
//     @Query("SELECT * FROM services")
//     suspend fun getCachedServices(): List<ServiceEntity>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheServices(services: List<ServiceEntity>)
// }
