package com.travelmonk.feature.experiences.data.local

// TODO: Implement local data source using Room when offline caching is added.
// This will hold a Room DAO for caching experiences by category.
//
// Example:
// @Dao
// interface ExperienceDao {
//     @Query("SELECT * FROM experiences WHERE category = :category")
//     suspend fun getCachedExperiences(category: String): List<ExperienceEntity>
//
//     @Insert(onConflict = OnConflictStrategy.REPLACE)
//     suspend fun cacheExperiences(experiences: List<ExperienceEntity>)
// }
